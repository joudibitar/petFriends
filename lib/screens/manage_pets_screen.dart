import 'package:flutter/material.dart';

class ManagePetsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Petleri Yönet'),
      ),
      body: Center(
        child: Text('Petleri yönetme ekranı burada olacak.'),
      ),
    );
  }
}
