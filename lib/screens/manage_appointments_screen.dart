import 'package:flutter/material.dart';

class ManageAppointmentsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Randevuları Yönet'),
      ),
      body: Center(
        child: Text('Randevuları yönetme ekranı burada olacak.'),
      ),
    );
  }
}
